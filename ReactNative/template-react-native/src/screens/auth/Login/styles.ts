import styled from "styled-components/native";
import { RFValue } from "react-native-responsive-fontsize";

export const LoginContainer = styled.View`
	flex: 1;
	background-color: #fff;
	align-items: center;
	justify-content: center;
	padding: 5%;
	gap: 12px;
`

export const LoginTextWrapper = styled.View`
	width: 68%;
	align-items: center;
	gap: 20px;
`

export const LoginFormWrapper = styled.View`
	width: 100%;
	align-items: center;
	gap: 40px;
`

export const LoginFormInputWrapper = styled.View`
	width: 100%;
	gap: 15px;
`

export const LoginRegisterLinkWrapper = styled.View`
	width: 100%;
	align-items: center;
	gap: 25px;
`

export const LoginNestedTextWrapper = styled.View`
	flex-direction: row;
	justify-content: center;
	align-items: center;
	gap: 5px
`